'use strict';

/**
 * mentoring-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::mentoring-request.mentoring-request');
